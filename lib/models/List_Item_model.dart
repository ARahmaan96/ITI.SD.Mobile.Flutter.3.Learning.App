class ListItemModel {
  String? originalText;
  String? translatedText;
  String? imagePath;
  String? audioPath;

  ListItemModel(
      {this.originalText, this.translatedText, this.audioPath, this.imagePath});
}
